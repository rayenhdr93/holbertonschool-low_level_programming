#include "main.h"
#include <string.h>
/**
 * _strstr - hi
 * @haystack: hi
 * @needle: hi
 * Return: hi
 */
char *_strstr(char *haystack, char *needle)
{
	unsigned int i, j, x, y;
	int a;

	j = 0;
	t =
	x = strlen(haystack);
	y = strlen(needle);
	for (i = 0; i < y; i++)
	{
	for (j = 0; j < (x - y); j++)
	{
		if (haystack[j] == needle[i])
		{
			
		}
	}
	}
	return (haystack);

}
