#include "stdlib.h"
#include "stdio.h"
/**
 * main - hi
 * @argc: hi
 * @argv: hi
 * Return: hi
 */
int main(int argc, char *argv[])
{
	int i = 0;

	for (i = 0; i < argc; i++)
	printf("%s\n", argv[i]);
	return (0);
}

